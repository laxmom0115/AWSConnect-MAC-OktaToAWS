{
  "name": "connect-provisioner",
  "version": "0.1.0",
  "description": "Webhook receiver Lambda — validates Okta Event Hook and enqueues provisioning tasks to SQS",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "20.x",
    "typescript": "~5.4.0"
  }
}
